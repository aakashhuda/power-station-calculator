# Improvements

- For the daily energy consumptions form, add a quantity field column. So for having
  3 fans or 2 lights consuming the same energy users don't have to type it separately. Alter the calculations if needed.
- Users can select currency to calculate the price. Tk, USD, GBP, EUR are the primary
  list of selections available. Default is Tk
- Change the Power Station form being aside of station area to a Modal/Dialog.
  Beside the stations tab having the same size there will be a button '+ Add Power Station'. Clicking that will open the modal. In the modal there will be two buttons.
  One +Add Station and the other is Cancel. The modal can only be closed using the Cancel button. This will also clear the form. +Add Station will add the station,
  reset the form and close the modal. The modal cannot be closed by clicking the mask.
  As the power station form will stay as a modal the Station Area should take the
  remaining space on the left.
- In the PDF, Daily Consumptions should be included. And also the Power Stations
  Summary should be included the way it has been shown in the web page. It won't be
  under a tabbed action as it is a PDF but the we have to create the PDF with such a
  structure so that the user can get all the important data summurized living in the website.
- In the comparison table the greatest runtime, solar charge time & ac charge time
  should be marked/labeled.
- Comparison table headers should not have (unit) inside the bracket. Rather the
  table datas should have units beside it for clearer understanding.
- For Headers, Labels, Buttons, capitalize the texts.
- Daily energy consumption line item delete icon should be increased in size
  appropriately.
- For number inputs, the step should be 0.5 not 1.
- Required inputs should have an asteriks with the label for improved UX.
