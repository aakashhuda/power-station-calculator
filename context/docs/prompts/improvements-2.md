# Improvements 2

- The Add Power Station modal's buttons are at the bottom of the modal.
  So when scrolled to the end of the modal it is seen. I want the buttons to always show
  within the view height of the modal. And the rest content can be scrolled.
- Instead of having Daily Energy form and Shared Settings side by side, I want you
  to put them one after another. The first form will be the daily energy consumption
  form. This way the Shared Settings won't strech it's height when the energy consumption
  form gets longer.
- The Best badge is taking too much space. Instead make the badge consist the value
  as text. This way it will stay within it's column space.
- The max-width value which is 100% is causing the device delete button look very small.
  Please get rid of the max-width.
- The PDF has now the infomration I needed. But their are a lot of unnecessary spaces.
  Currently their is a full page that stays empty when generated. Make the space allotment
  better for the PDF.
